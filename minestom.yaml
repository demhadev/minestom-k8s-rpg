server:
  # Keep these generic; let Docker handle the specifics
  compression-threshold: 256
  view-distance: 8
  simulation-distance: 6

# You can add custom settings here that are shared across all roles
game:
  exp-multiplier: 1.5